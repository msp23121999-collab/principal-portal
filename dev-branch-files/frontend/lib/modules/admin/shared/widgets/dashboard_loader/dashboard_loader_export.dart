import 'package:flutter/material.dart';

class DashboardLoaderSystem extends StatelessWidget {

  const DashboardLoaderSystem({super.key, required this.child});
  final Widget child;

  @override
  Widget build(BuildContext context) => child;
}
